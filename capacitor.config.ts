import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'boilerplate-monorepo',
  webDir: 'dist/boilerplate-monorepo',
  bundledWebRuntime: false,
};

export default config;
